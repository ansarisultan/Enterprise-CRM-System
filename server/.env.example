# MongoDB Atlas connection string
# Replace <username>, <password>, and <cluster-url> with your Atlas credentials.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/enterprise-crm?retryWrites=true&w=majority
